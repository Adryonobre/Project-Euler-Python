for b in range(0, 1000):
    for a in range(0, b):
        c = math.sqrt(a**2 + b**2)
        if (a + b + c == 1000):
             resultado = a * b * c
             print(resultado)
             print(a,b,c)
             break
